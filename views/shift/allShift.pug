extends ../layout

block content
    h1 All Shifts
    table.table
        thead.thead-dark
            tr
                th ID
                th User ID
                th Start Time
                th End Time
                th Actions
        tbody
            each shift in shifts
                tr
                    td #{shift.id}
                    td #{shift.user_id}
                    td #{shift.start_time}
                    td #{shift.end_time}
                    td
                        div.d-flex
                            a.btn.btn-primary.mr-2(href=`/shift/${shift.id}`) View
                            form(method='POST' action=`/shift/${shift.id}?_method=DELETE`)
                                input.btn.btn-danger(type='submit', value='Delete')
    a.btn.btn-success(href='/user/new') Create New Shift